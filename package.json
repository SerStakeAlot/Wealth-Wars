{
  "name": "wealth-wars",
  "version": "2.0.0",
  "description": "Premium financial strategy game on Solana - Current Alpha with gold-bordered dark theme",
  "scripts": {
    "build:program": "anchor build",
    "test:program": "anchor test",
    "dev:validator": "solana-test-validator -r --reset",
    "dev:web": "npm --prefix apps/web run dev",
    "generate:tokens": "npm --prefix apps/web run export:tokens"
  },
  "workspaces": [
    "apps/web"
  ]
}
